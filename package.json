{
  "name": "primestack-moto-pos",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node scripts/use-production-schema.js && npx prisma generate && npx prisma db push && node seed.js && node server.js",
    "dev": "node server.js",
    "build": "node scripts/use-production-schema.js && npx prisma generate && npx prisma db push",
    "migrate:dev": "npx prisma migrate dev",
    "migrate:prod": "npx prisma migrate deploy",
    "seed": "node seed.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-mongo-sanitize": "^2.2.0",
    "express-rate-limit": "^7.4.1",
    "helmet": "^8.0.0",
    "hpp": "^0.2.3",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.13.3",
    "prisma": "^5.22.0",
    "stripe": "^17.0.0",
    "winston": "^3.19.0",
    "zod": "^3.23.8"
  }
}
